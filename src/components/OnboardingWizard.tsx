/**
 * Onboarding wizard disabled – reserved for future redesign.
 * Renders nothing so the component can stay in the tree without showing UI.
 */
export function OnboardingWizard() {
  return null;
}
