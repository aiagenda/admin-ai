[build]
command = ""

[deploy]
import_map = ""

[functions]
# JWT is verified manually in the function body via supabase.auth.getUser()
# Set to false so the gateway doesn't reject requests before the function can
# return a proper error message (e.g. for CORS pre-flight responses)
verify_jwt = false
