# US test documents (GovLetter)

See GovLetter_US_test_pack.zip in test-documents/ or regenerate:

    python3 scripts/generate_us_test_pdfs.py

## Invoice / bookkeeping OCR samples

`invoices/` — US typed PDFs + handwritten-style PNGs for könyvelés testing:

    python3 scripts/generate_us_invoice_samples.py

Zip: `invoices/GovLetter_US_invoice_test_pack.zip`
